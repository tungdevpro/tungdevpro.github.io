---
export interface Props {
  noPadding?: boolean;
  ariaHidden?: boolean;
}

const { noPadding = false, ariaHidden = true } = Astro.props;
---

<div class={`mx-auto max-w-5xl ${noPadding ? "px-0" : "px-4"}`}>
  <hr class="border-skin-line/70" aria-hidden={ariaHidden} />
</div>
